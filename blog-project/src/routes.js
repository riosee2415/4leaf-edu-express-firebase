// global
const HOME = "/";
const ABOUT = "/about";
const POST = "/post";
const ETC = "/etc";

//routes
const routes = {
  home: HOME,
  about: ABOUT,
  post: POST,
  etc: ETC
};

export default routes;
